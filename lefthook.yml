pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{js,ts,mjs,cjs,json,css,md,yml,yaml}"
      run: npx oxfmt --check {staged_files}
    lint:
      glob: "*.{js,ts,mjs,cjs}"
      run: npx oxlint {staged_files}

pre-push:
  commands:
    check:
      run: npm run check
